"""Shared constants for cross-module wording and boundary messaging.

This module centralizes repeated text so deterministic outputs and agent/LLM
artifacts use consistent language. It does not contain runtime logic.
"""

from __future__ import annotations

CONFIG_PROVENANCE_CAVEAT = "Semantic role was provided by config; confidence reflects user-provided context, not deterministic inference."
