# Interpreting Outputs

`profiling_trace.json` contains:

- Dataset metadata (rows, columns, names, source file info)
- Column-level profiling (nulls, distinct counts, inferred physical type, sample values, top values)

Treat this artifact as structural evidence for future inference and dictionary generation.
