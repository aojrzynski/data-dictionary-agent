# Extension Paths

Future logic can be added in:

- `profiling.py` for richer deterministic profiling rules
- New inference modules for semantic role proposals
- Config loading modules for rule overrides
- Future agent modules for bounded orchestration
- Future rendering modules for dictionary outputs
