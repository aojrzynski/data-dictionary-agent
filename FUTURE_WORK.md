# Future Work

- Semantic inference rules (identifier/date/measure/category candidates)
- Data dictionary outputs (Markdown/CSV/JSON)
- Config overrides (`suggested_overrides.yaml`)
- Bounded agent mode with trace-driven actions
- Optional LLM-suggested descriptions grounded in trace facts
- Possible reviewer-loop/framework version for larger pipelines
